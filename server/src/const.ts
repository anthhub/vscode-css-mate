
export enum Ext {
    NAME = "css-mate"
}

export enum Cmd {
    LINT_CLASS = `lint_class`,
    LIST_AVAILABLE = "list_available",
    REPLACE_TO_RECOMMEND = "replace_to_recommend",
}

export const SPINNER = "$(sync~spin)"
export const WARNING = "$(warning)"
export const SUCCESS = "$(check)"
